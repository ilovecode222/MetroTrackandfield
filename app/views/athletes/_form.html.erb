<%= form_with(model: athlete, local: true) do |form| %>
  <% if athlete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(athlete.errors.count, "error") %> prohibited this athlete from being saved:</h2>

      <ul>
        <% athlete.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"name (First and Last)" %>
    <%= form.text_field :name %>
  </div>



  <div class="field">
    <%= form.label :school %>
    <%= form.select :school, options_for_select(['Other','Winston Prep', 'OCS', 'Lowl', 'Summit Queens'], athlete.school) %>
  </div>

  <div class="field">
    <%= form.label :"Date of Graduation" %>
    <%= form.date_field :grade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
